import React from 'react';
import './Header.css';

// Dumb component / Componente de presentación
// No tiene estado
const Header = () => {
  return (
    <header>
      <h1>Eventos</h1>
    </header>
  );
};

export default Header;
